# Project handoff — T4 RMS DV filtering

## Project

Acute_Crime is the Hackensack Police Department T4 Hotspot Analysis workspace: it documents standards, builds a PII-safe domestic-violence case blocklist for RMS exclusion, and ships Python under `Scripts/t4/` to normalize RMS/CAD columns, flag DV by type and blocklist, and integrate exclusion before Tier 2 scoring with data-quality JSON output.

## Active Branch / Working Files

- Branch: `main` (tracking `origin/main`; last pushed commit `273042c`).
- **This handoff session** only adds:
  - `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Docs\handoffs\session_state.json`
  - `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Docs\handoffs\handoff_20260416_2115.md`
- **Modified locally, not committed** (treat as in-flight until committed or reverted):
  - `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\CHANGELOG.md`
  - `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\CLAUDE.md`
  - `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Scripts\t4\type_fallback.py`

## What Was Completed This Session

- Wrote `session_state.json` and this handoff file under `Docs/handoffs\` with TODO status and resume steps.
- Prior pushed work (commit `273042c` on `main`): documentation for cycle ID strategy, RMS source defaults (local vs AGOL), Feb 2026 RMS file verified at 539 KB, and related doc alignment (`Docs/t4_cycle_id_strategy.md`, `Docs/t4_config_and_aliases.md`, `Docs/dv_blocklist_refresh_governance.md`, etc.).

## Where We Stopped

Work on **`Scripts/t4/type_fallback.py`** ended with the **standalone `__main__`** block that loads `incident_type_map.csv` and `CallType_Categories.csv` from OneDrive `02_ETL_Scripts` / `09_Reference`, builds `dv_set` via `build_dv_type_set`, and prints regex / test matches. Core functions `flag_dv_by_type` (from line 87) and `is_dv_type` are in place; uncommitted edits touch this file and top-level docs.

```118:149:C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Scripts\t4\type_fallback.py
# ── Standalone test ──────────────────────────────────────────────────
if __name__ == '__main__':
    from pathlib import Path

    ONEDRIVE = Path(r'C:\Users\carucci_r\OneDrive - City of Hackensack')
    ITM = ONEDRIVE / '02_ETL_Scripts' / 'dv_doj' / 'docs' / 'mappings' / 'incident_type_map.csv'
    CTC = ONEDRIVE / '09_Reference' / 'Classifications' / 'CallTypes' / 'CallType_Categories.csv'

    dv_set = build_dv_type_set(ITM, CTC)
    ...
    for v in test_values:
        result = is_dv_type(v, dv_set)
        print(f'  {str(v):45s} -> {"DV" if result else "---"}')
```

## Next Steps (Priority Order)

1. **Review and commit or discard** local changes to `CHANGELOG.md`, `CLAUDE.md`, and `Scripts/t4/type_fallback.py` so `main` matches the intended narrative and tests.
2. **Sync plan TODO IDs** in `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Docs\plans\t4_rms_dv_filtering_d5a59b9b.plan.md` with reality: mark **`type-fallback`** and **`score-integration`** completed or explicitly list remaining verification (E2E run, Tier 2 wiring).
3. **Run the pipeline** from repo root: `python -m Scripts.t4.score_integration --help` then a full run with `--cycle-id`, `--rms-path`, `--cad-path`, `--dv-blocklist` pointing at `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Data\dv_case_numbers_for_t4.csv` (ensure `Data\cad` and `Data\rms` exist or expect exit in `run_pipeline`).
4. **`confirm-rms-source`**: confirm operators use defaults in `Docs/t4_config_and_aliases.md` (local GDB export vs AGOL) before production runs.
5. **`refresh-governance`**: when opening a new calendar year or changing T4 windows, regenerate the DV roster and align `backfill_dv` `ValidationConfig.date_end` with `rms_pull_start` / analysis window (see `Docs/dv_blocklist_refresh_governance.md`).

## Open Decisions / Blockers

- **[DECISION NEEDED]** Whether Tier 2 automation will use a **calendar file** for `cycle_id` or **Section 0 only**, per `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Docs\t4_cycle_id_strategy.md` (R. A. Carucci).
- **[DECISION NEEDED]** Commit message and scope for pending `CHANGELOG.md` / `CLAUDE.md` / `type_fallback.py` edits before the next push.

## Known Risks

- **DV roster lag:** If `backfill_dv` `ValidationConfig.date_end` is before `rms_pull_start` or the T4 analysis window, newer DV cases may be missing from `Data/dv_case_numbers_for_t4.csv` — refresh roster and blocklist per governance doc.
- **Column names:** After `normalize_columns`, RMS uses snake_case; pitfalls for raw exports (`Case Number` vs `CaseNumber`) are documented — align with `Scripts/t4/column_norm.py` and `map_dv_to_rms_locations.py` patterns.
- **Missing local Data:** `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Data\cad` and `Data\rms` may be absent or empty (`.gitignore`); `score_integration.run_pipeline` validates paths and exits early.
- **Source data:** Do not copy `dv_final_enriched.csv` into Acute_Crime; use the merged export at `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Data\dv_case_numbers_for_t4.csv` only.

## How to Resume

1. Open `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Docs\handoffs\session_state.json` and this file; run `git status` in `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime`.
2. Confirm `C:\Users\carucci_r\OneDrive - City of Hackensack\10_Projects\Acute_Crime\Data\dv_case_numbers_for_t4.csv` exists and row counts match expectations; confirm DV roster date range vs T4 window.
3. Verify OneDrive paths for `incident_type_map.csv` and `CallType_Categories.csv` exist if running `type_fallback.py` as `__main__`.
4. Set `--cycle-id` and date args consistently with `Docs/t4_cycle_id_strategy.md`; ensure `Data\cad` and `Data\rms` inputs are populated for a full `score_integration` run.
5. Link-only context (do not re-summarize in session): `T4_Hotspot_Analysis_Master_Prompt_v3.md`, `Docs/plans/t4_rms_dv_filtering_d5a59b9b.plan.md`; reuse `standardise_case_number` / `CASE_PATTERN` from `02_ETL_Scripts/dv_doj/etl_scripts/backfill_dv.py` and column pitfalls from `map_dv_to_rms_locations.py`.
